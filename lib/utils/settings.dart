// Agora App ID (App ID only project - no certificate)
const appId = 'c5462cf0db96452fae1b2af34d163fde';

// No-token mode: leave empty string when using an "App ID only" project.
// If you later enable certificate, fetch a token from your server per channel.
const token = '';

// Optional: force a fixed channel id for testing. Leave empty to use per-session channel ids.
const defaultChannelId = '';